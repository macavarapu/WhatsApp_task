



import 'package:flutter/material.dart';

import '../vido_call_screen/vido_screen.dart';

class Dailycall extends StatelessWidget {
  const Dailycall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.teal,
      body: Body(),
    );
  }
}
